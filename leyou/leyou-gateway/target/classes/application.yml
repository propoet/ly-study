server:
  port: 10011
spring:
  application:
    name: leyou-gateway
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10088/eureka
    registry-fetch-interval-seconds:  5
zuul:
  prefix: /api      # 路由路径前缀